import * as vscode from "vscode";
import { KinClient, KinEntity } from "./kin-client";
type ExplorerNode = KindGroupNode | EntityNode;
interface KindGroupNode {
    type: "kind";
    kind: string;
    count: number;
}
interface EntityNode {
    type: "entity";
    entity: KinEntity;
}
export declare class EntityExplorerProvider implements vscode.TreeDataProvider<ExplorerNode> {
    private client;
    private workspacePath;
    private _onDidChangeTreeData;
    readonly onDidChangeTreeData: vscode.Event<void | ExplorerNode | undefined>;
    private entities;
    private grouped;
    constructor(client: KinClient, workspacePath: string);
    refresh(): void;
    getTreeItem(element: ExplorerNode): vscode.TreeItem;
    getChildren(element?: ExplorerNode): Promise<ExplorerNode[]>;
    private getKindGroups;
}
export {};
//# sourceMappingURL=entity-explorer.d.ts.map