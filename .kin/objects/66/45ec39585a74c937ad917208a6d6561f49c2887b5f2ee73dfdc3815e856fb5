export interface KinEntity {
    kind: string;
    name: string;
    file: string;
    line: number;
    signature?: string;
}
export interface KinStatus {
    initialized: boolean;
    entityCount: number;
    graphState: string;
}
export interface KinOverview {
    entities: number;
    edges: number;
    files: number;
    kinds: Record<string, number>;
}
export declare class KinClient {
    private binaryPath;
    private workspacePath;
    constructor(workspacePath: string);
    private resolveBinary;
    /**
     * Execute the kin binary with the given arguments.
     * Uses execFile (NOT exec) to avoid shell injection.
     */
    private run;
    private runJson;
    search(query: string): Promise<KinEntity[]>;
    entities(): Promise<KinEntity[]>;
    overview(): Promise<KinOverview>;
    trace(entity: string): Promise<KinEntity[]>;
    status(): Promise<KinStatus>;
    init(): Promise<string>;
    isAvailable(): boolean;
}
//# sourceMappingURL=kin-client.d.ts.map